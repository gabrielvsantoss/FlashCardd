@model PerguntasERespostasGepetecoViewModel

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Cadastrando tema de card";
}

<hr />
<div class="row justify-content-center my-5">
    <div class="col-md-8">
        <div class="card rounded-4 border-0">
            <div class="card-body p-md-5">


                <div class="justify-content-center">
                    <a class="btn btn-primary rounded-3 px-5"  href="/api/texto-com-gpt/@(Uri.EscapeDataString(Model.PerguntaCompleta))">Mandar tema para IA realizar a criação dos cards</a>

                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}